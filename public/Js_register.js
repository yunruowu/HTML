var password = document.getElementsByName("password")
var check_password = document.getElementsByName("repeat_password")
console.log(password.value)
ss  = password.value
function check_p(){

    if(password.values != check_password.values){
        alert("shihc")
    }
    
}
